{
  "name": "atiendo-boludos-quiz",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /atiendo-boludos-q",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0"
  }
}
